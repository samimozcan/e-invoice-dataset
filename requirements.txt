# DeepSeek OCR Training Requirements
# Python 3.10+ recommended (tested with Python 3.10.12)
# CUDA 12.1+ required for RTX 4090

# Core ML frameworks
torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0

# Transformers and training
transformers>=4.40.0
accelerate>=0.27.0
datasets>=2.18.0

# QLoRA / PEFT for efficient fine-tuning
peft>=0.10.0
bitsandbytes>=0.43.0

# Image processing
Pillow>=10.0.0
opencv-python>=4.8.0

# Utilities
tqdm>=4.66.0
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0

# Logging and monitoring
wandb>=0.16.0
tensorboard>=2.15.0

# Flash Attention 2 (install separately)
# pip install flash-attn --no-build-isolation

# Jupyter
jupyter>=1.0.0
ipywidgets>=8.0.0
